// Package rule defines the detection rule types and DSL used by the
// R3TRIVE rule engine.
//
// See RULE_ENGINE_SPEC.md for the full rule language specification.
package rule

// TODO: Implement rule types and DSL:
// - Rule struct (YAML-serializable)
// - Condition types (field match, temporal sequence, threshold)
// - Operator types (eq, ne, contains, regex, gt, lt, oneOf, noneOf)
// - Macro system for reusable condition fragments
// - Rule loading from YAML files
// - Rule validation and syntax checking
// - R3T-{CATEGORY}-{NNN} ID format enforcement
